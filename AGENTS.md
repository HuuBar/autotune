# 背景
metro_arrive(地铁到站预测) 项目：通过将用户历史乘车数据集群化，配合时间特征，训练神经网络模型，用于预测用户未来地铁下车站点。
注意：当前任务指令只与cluster构建有关，与神经网络的训练无关。
你只需关注 `cluster/` 目录下的代码，禁止修改 `neural_network/`目录下的代码。

# 指令
当前状态指标：cluster的覆盖率为79.37%，轮廓系数为0.7982
最终优化目标：将cluster的覆盖率提升至82%，且轮廓系数不要低于0.72
测试文件：`cluster/run.py`

# 特殊说明
在开始行动之前，你必须先仔细思考一下自己的**工作流**是什么，并严格遵循工作流中的步骤。
